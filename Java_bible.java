Java_bible:

--to print in to the console:
Console console = System.console();
console.print();

--to exit from the console:
System.console(0;

if and else:
	if (variable < x) {

	}

--Parsing String to integer
String age1 = console.readLine("your number")
int age2 = ParsingInt(age1);

